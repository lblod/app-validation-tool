
version: '3.7'
x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
services:
  identifier:
    environment:
      VIRTUAL_HOST: "validatietool.lblod.info"
      LETSENCRYPT_HOST: "validatietool.lblod.info"
      LETSENCRYPT_EMAIL: "lennybontenakel@specular.io"
    networks:
      - default
      - proxy
    labels:
      - "logging=true"
networks:
  proxy:
    external:
      name: letsencrypt_default
